image: node:14

stages:
  - build
  - test

backend_build:
  stage: build
  script:
    - cd backend
    - npm install

backend_test:
  stage: test
  script:
    - cd backend
    - npm test

  artifacts:
    when: always
    reports:
      junit:
        - junit.xml
